# Modelo de datos

La persistencia concreta todavia no esta acoplada al dominio. Los repositorios de salida definen las operaciones necesarias y los adaptadores se implementan en infraestructura.

Estado actual: `InMemoryProductRepository` permite validar el flujo. Pendiente: entidades de persistencia, migraciones y adaptadores SQL Server.
